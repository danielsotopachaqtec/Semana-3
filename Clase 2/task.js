// Crear una función que eleve al cuadrado y al cubo un numero enviado como parámetro a dicha función esta debe ser una variable.

var initialNumber = 4;

function elevate(numberToElevate) {
	// operations
	console.log(numberToElevate);
}

elevate(initialNumber);

var initialNumber = 9;

var initialNumber = 12;

var initialNumber = 64;

var initialNumber = 64;
